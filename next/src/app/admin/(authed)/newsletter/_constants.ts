/* ══════════════════════════════════════════
   _constants.ts — /admin/newsletter 공유 상수 (S250-2 Phase 2)

   Next.js 16 `'use server'` 파일은 export async function 만 허용
   (feedback_use_server_async_only). 버킷 상수는 본 모듈로 격리.
   ══════════════════════════════════════════ */

/** Supabase Storage 버킷 — 뉴스레터 본문 이미지 (083 마이그). */
export const NEWSLETTER_IMAGES_BUCKET = 'newsletter-images';
